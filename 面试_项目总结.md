# 基于预训练大语言模型的航空发动机剩余使用寿命预测

**2026.02 – 2026.07 | 自主科研项目（论文复现 + 方法改进）**

---

## 一、数据工程

基于 NASA C-MAPSS 航空发动机数据集（FD001–FD004，4 种工况-故障组合），构建面向时序预测的预处理流水线。

**预处理流程：**
1. **工况聚类与自适应归一化**：对多工况数据集（FD002/FD004），以 3 个运行参数为特征执行 KMeans 聚类（k=6），识别 6 种运行模态；每种模态内独立完成 Z-score 标准化，再执行全局 MinMax 归一化至 [0,1]
2. **特征选择**：计算 21 个传感器的全局 Pearson 相关系数（传感器值 vs 运行时间）与逐发动机单调性得分，加权组合（α=0.5）后以 Cri > 均值准则筛选，保留 13–14 个最具退化指示能力的传感器
3. **滑动窗口构造**：以步长 stride=1 滑窗切分时序数据（窗口长度 FD001=30, FD002/004=64, FD003=40），训练集 RUL 标签采用分段线性衰减并截断至 125 周期
4. **训练/验证划分**：采用 Random Engine Split（按发动机 ID 随机 80/20 分割），杜绝同一发动机跨集合出现的数据泄漏

---

## 二、模型设计

### 2.1 基线模型：GPT4RUL（Tan et al., QR2MSE 2025）

```
(B, T, F) 传感器窗口
  ↓ Patching — 滑动分块（patch_size=8, stride=4/8）
  ↓ Linear Proj — 线性投影至 128 维 + 可学习位置编码
  ↓ PCF Block × N — MLP-Mixer 风格，Pre-LN 双独立 LayerNorm
  │   ├─ Patch-Mixing MLP：跨 patch 交互（时间维度）
  │   └─ Channel-Mixing MLP：跨通道交互（特征维度）
  ↓ Linear Mapping — 128 → 768，桥接至 GPT-2 隐空间
  ↓ Frozen GPT-2（仅前 3 层）— 预训练表征提取
  ↓ Flatten & LayerNorm → Linear(1) → RUL
```

### 2.2 改进模型：Hybrid Prompt RUL（受 SOC 论文启发的软提示注入）

在 GPT4RUL 基础上引入**可训练软提示（Soft Prompts）**——一组 M 个 768 维的可学习向量，拼接到传感器特征序列前方，与数据一同进入冻结的 GPT-2。软提示在训练中学会引导 GPT-2 关注当前任务的退化模式，作用类似"调节旋钮"。

```
传感器特征 (B, P, 768)
  ↓ [Soft Prompts (B, M, 768); Sensor Features (B, P, 768)]
  ↓ → 扩展序列 (B, M+P, 768)
  ↓ Frozen GPT-2 → 切掉软提示部分 → 保留数据部分
  ↓ Flatten → Linear(1) → RUL

仅训练：PCF + Linear + Soft Prompts + Output Head（~95K 参数）
GPT-2：完全冻结
```

### 2.3 消融基线：No-GPT2

去掉 GPT-2 和软提示，PCF → Flatten → Linear(1)，验证 LLM 的真实贡献。

---

## 三、实验

### 3.1 复现诊断（12+ 项对照实验）

| 排查项 | 尝试 | 结论 |
|--------|------|------|
| PCF LayerNorm 结构 | 单共享 LN → 双独立 Pre-LN | ✅ 与 MLP-Mixer 标准对齐 |
| 验证集数据泄漏 | val_last_idx 未过滤训练引擎 | ✅ 修复 |
| 验证评估方式 | last_window → 全部滑窗 | ✅ 消除虚假 val_rmse=0 |
| 特征选择 Corr 计算 | 逐引擎平均 → 全局 Pearson | ✅ FD003/004 改善 |
| FD004 特征数 | threshold → top-14 | ✅ FD004 14.31→13.97 |
| FD003 强制 14 特征 | 13→14 | ❌ 自然最优值即 13 |
| 特征选择范围 | train+test 全集 | ❌ 引入噪声 |
| Test RUL 截断 | 取消截断 | ❌ FD002/004 崩溃 |
| Dropout | 0.2→0.3 | ❌ 恶化 |
| 学习率 | 0.002/0.004 | ❌ 均不如 0.005 |
| 数据划分 | temporal split | ❌ 同发动机切分=数据泄漏 |
| **Condition Embedding** | 工况参数加入输入 | ✅ 全部 4 数据集改善 |

### 3.2 消融实验：GPT-2 到底有没有用？

| 数据集 | 工况/故障 | GPT4RUL+CE | No-GPT2 (纯PCF) | Hybrid+SP (M=4) | 论文 |
|--------|-----------|-----------|-----------------|-----------------|------|
| FD001 | 1×1 | 14.87 | 14.16 | **13.80** | ~11.2 |
| FD002 | 6×1 | 12.87 | 13.61 | **12.55** | ~12.6 |
| FD003 | 1×2 | 13.69 | **12.99** | 13.59 | ~13.2 |
| FD004 | 6×2 | **13.38** | 14.52 | 13.76 | ~12.9 |

### 3.3 核心发现

1. **LLM 的价值取决于任务复杂度**：简单场景下 PCF 已足够（FD003 最优=纯 PCF 12.99），多工况下 GPT-2 + 软提示才发挥优势（FD001/002 Hybrid 最优）
2. **软提示是激活 LLM 能力的关键**：不加软提示时 GPT-2 甚至在拖后腿（FD001: 14.16→14.87→13.80），软提示让 LLM 从"噪声源"变成"增强器"
3. **FD002 首次且唯一超越论文**（12.55 < 12.6）
4. **没有万能架构**：4 个数据集的胜出模型各不相同，需要根据任务复杂度选择

---

## 四、技术栈与能力

- **框架**：PyTorch, HuggingFace Transformers (GPT-2)
- **数据处理**：NumPy, Pandas, scikit-learn, SciPy
- **实验管理**：模块化配置系统，JSON 结果记录，CSV 汇总
- **能力**：论文复现、系统诊断、消融实验设计、LLM 适配（Prompt Tuning）
